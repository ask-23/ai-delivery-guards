name: AI Delivery Guards

on:
  pull_request:
    branches: [main]

jobs:
  ai-delivery-guards:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - run: go run ./cmd/guard check --root .
